# WhisperX - Enhanced Whisper with batching, alignment, and speaker diarization
whisperx
torch>=2.0.0
torchaudio>=2.0.0

# Additional dependencies for WhisperX
pyannote.audio  # Integrated with WhisperX for speaker diarization
faster-whisper  # Used internally by WhisperX

# Audio processing
ffmpeg-python

# CUDA support (uncomment if using NVIDIA GPU)
# --extra-index-url https://download.pytorch.org/whl/cu118

# Crawl4AI - Web scraping with JavaScript rendering
crawl4ai
playwright  # Browser automation for Crawl4AI

# Marker - PDF to Markdown conversion
marker-pdf  # Basic PDF conversion
# marker-pdf[full]  # Uncomment for non-PDF document support (DOCX, PPTX, etc.)
