'use client';
interface MapBackgroundPickerProps {
  open: boolean;
  onDone: () => void;
  campaignId: string;
  mapId?: string;
  slug: string;
}
export function MapBackgroundPicker(_props: MapBackgroundPickerProps) { return null; }
