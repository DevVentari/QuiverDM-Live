{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'No build needed - using Railway for databases only'"
  },
  "deploy": {
    "startCommand": "echo 'No app deployment - using Railway for PostgreSQL and Redis only'",
    "restartPolicyType": "never"
  }
}
